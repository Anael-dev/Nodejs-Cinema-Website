<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
  
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <!-- CSS -->  </head>
  <body>
    <div class="mt-2 pb-2" style="width:90vw">
      <h5 class="font-weight-light"><%=user.fullName%></h5>
      <%-include title %>
          <nav class="navbar d-flex justify-content-between navbar-expand-md navbar-light bg-light" style="width:90vw">
          <div class="navbar-nav">
            <a class="nav-item nav-link " href="/movies">Movies</a>
            <a class="nav-item nav-link" href="/subscriptions">Subscriptions</a>
            <% if(user.admin){ %>
            <a class="nav-item nav-link" href="/users">Users Management</a>
            <% } %>
            <a class="nav-item nav-link" href="/login/logout">Log Out</a>
          </div>
      </nav>
      
        <% let typeCapitalized;%>
        <%if (type){ %>
        <%if (type=="member"){ %>
        <% typeCapitalized='Subscription' %>
        <% } else {   %>
          <% typeCapitalized = type.charAt(0).toUpperCase() + type.slice(1) %>
          <% } %>
          <h4 class="mt-4" ><%=typeCapitalized%>s</h4>
        <% }         %>      
    </div>
  </body>
</html>




